export const environment = {
  production: true,
  // Replaced at build time by the deploy-frontend workflow with the
  // API_BASE_URL repository variable (e.g. https://braindump-api.azurewebsites.net).
  apiBaseUrl: '__API_BASE_URL__',
};
